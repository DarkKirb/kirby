language: python
python:
    - "3.5"
    - "3.5-dev"
    - "3.6"
    - "3.6-dev"
    - "3.7-dev"
    - "pypy3.5"

install:
    pip install -r requirements.txt
    pip install .

script: pytest --cov kirby
after_success:
  - coveralls
